# 🕸️ Scraper

### Run with Docker

```bash
docker image build -t scraper-rb .
```

```bash
docker container run -it scraper-rb
```

### Run without Docker
Install dependencies
```bash
bundle install
```
Run `app.rb`
```bash
ruby app.rb
```